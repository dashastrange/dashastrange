name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"  # every 12 hours
  workflow_dispatch:

jobs:
  build:
    name: Generate pink snake with black and green tiles
    runs-on: ubuntu-latest

    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: dashastrange
          outputs: |
            dist/github-snake-night.svg?palette=custom&color_snake=#FF6EC7&color_dots=#1B1F23,#2A2D36&color_tiles=#00FF00,#1A1A1A
      - name: Push to GitHub Pages
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
